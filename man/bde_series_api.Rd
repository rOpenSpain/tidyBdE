% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bde-series-api.R
\encoding{UTF-8}
\name{bde_series_api}
\alias{bde_series_api}
\alias{bde_series_api_latest}
\alias{bde_series_api_load}
\title{Load BdE time series from the Statistics web service (API)}
\usage{
bde_series_api_latest(series_code, language = c("en", "es"), verbose = FALSE)

bde_series_api_load(
  series_code,
  series_label = NULL,
  out_format = "wide",
  language = c("en", "es"),
  time_range = NULL,
  verbose = FALSE,
  extract_metadata = FALSE
)
}
\arguments{
\item{series_code}{A character vector of API series codes from the
\code{Nombre_de_la_serie} field of the corresponding catalog. These values are
passed to the API \code{series_list} parameter; they are not the stable
sequential numbers used by \code{\link[=bde_series_load]{bde_series_load()}}.}

\item{language}{A string specifying the output language: \code{"es"} for Spanish
or \code{"en"} for English.}

\item{verbose}{Logical. If \code{TRUE}, display informative messages.}

\item{series_label}{An optional character vector of labels to assign to the
extracted series.}

\item{out_format}{The output format, either \code{"wide"} or \code{"long"}. See
\strong{Value} for details and the \strong{Examples} section.}

\item{time_range}{An optional string specifying a year or API range code. It
can be a year, such as \code{"2024"}, or one of \code{"3M"}, \code{"12M"}, \code{"30M"},
\code{"36M"}, \code{"60M"} or \code{"MAX"}. If \code{NULL}, the API returns the smallest range
for the series frequency. Range codes are validated against the frequency
returned by \code{\link[=bde_series_api_latest]{bde_series_api_latest()}}. See \strong{Details}.}

\item{extract_metadata}{Logical. If \code{TRUE}, return the metadata associated
with the requested series.}
}
\value{
\code{bde_series_api_latest()} returns a \link[tibble:tbl_df]{tibble} with the latest
published observation for each valid series. It includes fields returned by
the Latest Data request such as \code{serie}, \code{descripcionCorta}, \code{codFrecuencia},
\code{decimales}, \code{simbolo}, \code{tendencia}, \code{fechaValor} and \code{valor}.

\code{bde_series_api_load()} returns a \link[tibble:tbl_df]{tibble}. When
\code{extract_metadata = FALSE}, API dates are parsed as \code{\link[=as.Date]{Date}}
values and observations are returned in wide or long format according to
\code{out_format}. When \code{extract_metadata = TRUE}, it returns one row per valid
series with fields returned by the Series List request, including
\code{fechaInicio}, \code{fechaFin} and metadata fields derived from \code{informacion}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

These functions retrieve BdE time series using the\href{https://www.bde.es/webbe/en/estadisticas/recursos/api-estadisticas-bde.html}{Statistics web service (API)}.

The API is a JSON web service that provides URL-based access to information
available in the Statistics section of Banco de España and the BIEST
application.

The API defines two request types. \code{\link[=bde_series_api_latest]{bde_series_api_latest()}} uses the Latest
Data request to obtain the latest published observation for one or more
series. \code{\link[=bde_series_api_load]{bde_series_api_load()}} uses the Series List request to obtain the
details of one or more complete series and their metadata.
}
\details{
Allowed \code{time_range} values depend on the series frequency:
\itemize{
\item Daily frequency (\code{D}): \code{"3M"} (last 3 months), \code{"12M"} and \code{"36M"}.
\item Monthly frequency (\code{M}): \code{"30M"}, \code{"60M"} and \code{"MAX"} (entire series).
\item Quarterly frequency (\code{Q}): \code{"30M"}, \code{"60M"} and \code{"MAX"}.
\item Annual frequency (\code{A}): \code{"60M"} and \code{"MAX"}.
}

If \code{time_range} is not specified, the request returns the smallest range for
the series frequency. For example, monthly series return \code{"30M"}.
}
\section{Series identifiers}{

BdE identifies each series with a stable sequential number
(\code{Numero_secuencial}) in bulk CSV files and an API series code
(\code{Nombre_de_la_serie}) in the Statistics web service.
\code{\link[=bde_series_load]{bde_series_load()}} accepts stable sequential numbers in \code{series_code}.
\code{\link[=bde_series_api_latest]{bde_series_api_latest()}} and \code{\link[=bde_series_api_load]{bde_series_api_load()}} use the same argument
for API series codes. Use \code{\link[=bde_catalog_load]{bde_catalog_load()}} or \code{\link[=bde_catalog_search]{bde_catalog_search()}} to
find both identifiers.
}

\examples{
\dontshow{if (bde_check_access()) withAutoprint(\{ # examplesIf}
\donttest{
xr <- bde_catalog_load(catalog = "TC")

# Extract the latest value.
library(dplyr)
xr |>
  slice_head(n = 3) |>
  pull(Nombre_de_la_serie) |>
  bde_series_api_latest(language = "en") |>
  glimpse()

# Extract the latest months.
xr |>
  slice_head(n = 1) |>
  pull(Nombre_de_la_serie) |>
  bde_series_api_load(language = "en", time_range = "12M") |>
  glimpse()

# Extract metadata.
xr |>
  slice_head(n = 1) |>
  pull(Nombre_de_la_serie) |>
  bde_series_api_load(
    language = "en", time_range = "12M",
    extract_metadata = TRUE
  ) |>
  glimpse()
}
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=bde_catalog_load]{bde_catalog_load()}} and \code{\link[=bde_catalog_search]{bde_catalog_search()}} help find API series codes.
\item \code{\link[=bde_series_load]{bde_series_load()}} loads time series from bulk CSV files.
}

Time series functions:
\code{\link{bde_series}}
}
\concept{series}
