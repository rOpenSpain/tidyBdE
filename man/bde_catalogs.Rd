% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bde-catalogs.R
\encoding{UTF-8}
\name{bde_catalogs}
\alias{bde_catalogs}
\alias{bde_catalog_load}
\alias{bde_catalog_update}
\alias{bde_catalog_search}
\title{Load, update and search BdE catalog metadata}
\source{
\href{https://www.bde.es/webbe/en/estadisticas/recursos/descargas-completas.html}{Banco de España time series bulk data download}.
}
\usage{
bde_catalog_load(
  catalog = c("ALL", "BE", "SI", "TC", "TI", "PB"),
  parse_dates = TRUE,
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE
)

bde_catalog_update(
  catalog = c("ALL", "BE", "SI", "TC", "TI", "PB"),
  cache_dir = NULL,
  verbose = FALSE
)

bde_catalog_search(pattern, ...)
}
\arguments{
\item{catalog}{A catalog identifier or \code{"ALL"} to load or update every
catalog. See \strong{Details}.}

\item{parse_dates}{Logical. If \code{TRUE}, date columns are parsed with
\code{\link[=bde_parse_dates]{bde_parse_dates()}}.}

\item{cache_dir}{Path to a cache directory. The directory can also be set
with \code{options(bde_cache_dir = "path/to/dir")}.}

\item{update_cache}{Logical. If \code{TRUE}, the requested file is refreshed in
\code{cache_dir}.}

\item{verbose}{Logical. If \code{TRUE}, display informative messages.}

\item{pattern}{Regular expression to search for. See \strong{Details} and
\strong{Examples}.}

\item{...}{Additional arguments passed by \code{bde_catalog_search()} to
\code{\link[=bde_catalog_load]{bde_catalog_load()}}.}
}
\value{
\code{bde_catalog_load()} returns a \link[tibble:tbl_df]{tibble} with the requested
time series catalog metadata. See
\code{vignette("csv_manual", package = "tidyBdE")} for details.

\code{bde_catalog_update()} returns an invisible list of download results.

\code{bde_catalog_search()} returns a \link[tibble:tbl_df]{tibble} with matching
catalog rows.
}
\description{
These functions manage BdE time series catalog metadata from the bulk CSV
files published by Banco de España.

\code{bde_catalog_load()} loads time series catalog metadata into a tibble,
\code{bde_catalog_update()} refreshes the cached catalog files and
\code{bde_catalog_search()} searches catalog metadata for keywords.
}
\details{
Accepted values for \code{catalog} are:\tabular{llll}{
   \strong{CODE} \tab \strong{PUBLICATION} \tab \strong{UPDATE FREQUENCY} \tab \strong{FREQUENCY} \cr
   \code{"BE"} \tab Statistical Bulletin \tab Daily \tab Monthly \cr
   \code{"SI"} \tab Summary Indicators \tab Daily \tab Daily \cr
   \code{"TC"} \tab Exchange Rates \tab Daily \tab Daily \cr
   \code{"TI"} \tab Interest Rates \tab Daily \tab Daily \cr
   \code{"PB"} \tab Bank Lending Survey \tab Quarterly \tab Quarterly \cr
}


Use \code{"ALL"} as a shorthand for loading or updating all catalogs at once.

If the requested catalog is not cached, \code{bde_catalog_load()} calls
\code{\link[=bde_catalog_update]{bde_catalog_update()}}.

\strong{Note:} BdE catalog metadata is currently available in Spanish only.
Therefore, search terms passed to \code{bde_catalog_search()} must be in Spanish
to retrieve results.

\code{bde_catalog_search()} uses \code{\link[base:grep]{base::grep()}} to find matches in the catalog
metadata. You can pass \link[base:regex]{regular expressions} to broaden the
search.
}
\examples{
\dontshow{if (bde_check_access()) withAutoprint(\{ # examplesIf}
\donttest{
bde_catalog_load("TI", verbose = TRUE)

# Simple search. Search terms must be in Spanish.
# PIB [es] == GDP [en].
bde_catalog_search("PIB")

# Search with a single complex condition.
bde_catalog_search("Francia(.*)PIB")

# Search with multiple complex conditions.
bde_catalog_search("Francia(.*)PIB|Italia(.*)PIB|Alemania(.*)PIB")

bde_catalog_update("TI", verbose = TRUE)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=bde_series_load]{bde_series_load()}} and \code{\link[=bde_series_full_load]{bde_series_full_load()}} load bulk CSV series.
\item \code{\link[=bde_series_api_load]{bde_series_api_load()}} and \code{\link[=bde_series_api_latest]{bde_series_api_latest()}} retrieve series
through the Statistics web service (API).
}
}
\concept{catalog}
