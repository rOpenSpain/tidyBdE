% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bde-series.R
\encoding{UTF-8}
\name{bde_series}
\alias{bde_series}
\alias{bde_series_load}
\alias{bde_series_full_load}
\title{Load BdE time series from bulk CSV files}
\source{
\href{https://www.bde.es/webbe/en/estadisticas/recursos/descargas-completas.html}{Banco de España time series bulk data download}.
}
\usage{
bde_series_load(
  series_code,
  series_label = NULL,
  out_format = "wide",
  parse_dates = TRUE,
  parse_numeric = TRUE,
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE,
  extract_metadata = FALSE
)

bde_series_full_load(
  series_csv,
  parse_dates = TRUE,
  parse_numeric = TRUE,
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE,
  extract_metadata = FALSE
)
}
\arguments{
\item{series_code}{A numeric vector of stable BdE sequential numbers, or
values coercible with \code{\link[base:as.double]{base::as.double()}}, from the \code{Numero_secuencial}
field of the corresponding series. This is not the API series code. See
\code{\link[=bde_catalog_load]{bde_catalog_load()}}.}

\item{series_label}{An optional character vector of labels to assign to the
extracted series.}

\item{out_format}{The output format, either \code{"wide"} or \code{"long"}. See
\strong{Value} for details and the \strong{Examples} section.}

\item{parse_dates}{Logical. If \code{TRUE}, date columns are parsed with
\code{\link[=bde_parse_dates]{bde_parse_dates()}}.}

\item{parse_numeric}{Logical. If \code{TRUE}, parse observation columns as double
vectors. See \strong{Note}.}

\item{cache_dir}{Path to a cache directory. The directory can also be set
with \code{options(bde_cache_dir = "path/to/dir")}.}

\item{update_cache}{Logical. If \code{TRUE}, the requested file is refreshed in
\code{cache_dir}.}

\item{verbose}{Logical. If \code{TRUE}, display informative messages.}

\item{extract_metadata}{Logical. If \code{TRUE}, return the metadata associated
with the requested series.}

\item{series_csv}{A bulk CSV file name for a series, as defined in the field
\code{Nombre_del_archivo_con_los_valores_de_la_serie} of the corresponding
catalog. See \code{\link[=bde_catalog_load]{bde_catalog_load()}} and the \strong{About BdE file naming}
section.}
}
\value{
\code{bde_series_load()} returns a \link[tibble:tbl_df]{tibble} with a \code{Date} column:
\itemize{
\item With \code{out_format = "wide"}, each series is presented in a separate column
with the name defined by \code{series_label}.
\item With \code{out_format = "long"}, the tibble has two additional columns:
\code{serie_name} contains the label of each series; \code{serie_value} contains the
corresponding value.
}

\code{"wide"} format is more suitable for exporting to a CSV file;
\code{"long"} format is more suitable for creating plots with
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}. See also \code{\link[tidyr:pivot_longer]{tidyr::pivot_longer()}} and
\code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}}.

\code{bde_series_full_load()} returns a \link[tibble:tbl_df]{tibble} with a \code{Date}
column and the aliases of the time series columns as described in catalog
metadata. See \code{\link[=bde_catalog_load]{bde_catalog_load()}} and
\code{vignette("csv_manual", package = "tidyBdE")} for details.
}
\description{
These functions load BdE time series from the bulk CSV files published by
Banco de España.

\code{bde_series_load()} extracts one or more series by their stable sequential
number. \code{bde_series_full_load()} loads a complete bulk CSV file and returns
all series included in that file.

See \code{vignette("csv_manual", package = "tidyBdE")} for details.
}
\note{
These functions attempt to parse columns as double values. For some time
series, a warning may be displayed if parsing fails. Set
\code{parse_numeric = FALSE} to disable numeric parsing.
}
\section{About BdE file naming}{

A series alias is a positional code that identifies the location, column or
row of a series in a table. An alias is unique within its context but is not
stable because it may change when a series moves.

A single time series may appear in different tables, so it can have several
aliases. Use \code{bde_series_full_load()} when you need to work with aliases or
load a complete file.

The series alias is also used in full CSV files. For example, table
\strong{be_6_1} represents Table 1, Chapter 6 of the Statistical Bulletin
("BE"). Although it is unique, it is subject to change, for example when a
new table is inserted before it.
}

\section{Series identifiers}{

BdE identifies each series with a stable sequential number
(\code{Numero_secuencial}) in bulk CSV files and an API series code
(\code{Nombre_de_la_serie}) in the Statistics web service.
\code{\link[=bde_series_load]{bde_series_load()}} accepts stable sequential numbers in \code{series_code}.
\code{\link[=bde_series_api_latest]{bde_series_api_latest()}} and \code{\link[=bde_series_api_load]{bde_series_api_load()}} use the same argument
for API series codes. Use \code{\link[=bde_catalog_load]{bde_catalog_load()}} or \code{\link[=bde_catalog_search]{bde_catalog_search()}} to
find both identifiers.
}

\examples{
\dontshow{if (bde_check_access()) withAutoprint(\{ # examplesIf}
\donttest{
# Show metadata.
bde_series_load(573234, verbose = TRUE, extract_metadata = TRUE)

# Load data.
bde_series_load(573234, extract_metadata = FALSE)

# Load multiple series.
bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR"),
  extract_metadata = TRUE
)

wide <- bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR")
)

# Show wide output.
wide

# Show long output.
long <- bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR"),
  out_format = "long"
)

long

# Use with ggplot2.
library(ggplot2)

ggplot(long, aes(Date, serie_value)) +
  geom_line(aes(group = serie_name, color = serie_name)) +
  scale_color_bde_d() +
  theme_tidybde()

# Show metadata for a complete bulk CSV file.
bde_series_full_load("TI_1_1.csv", extract_metadata = TRUE)

# Load a complete bulk CSV file.
bde_series_full_load("TI_1_1.csv")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=bde_catalog_load]{bde_catalog_load()}} and \code{\link[=bde_catalog_search]{bde_catalog_search()}} help find stable sequential
numbers.
\item \link[=bde_indicators]{Indicator wrappers} retrieve commonly used Spanish
macroeconomic series.
}

Time series functions:
\code{\link{bde_series_api}}
}
\concept{series}
