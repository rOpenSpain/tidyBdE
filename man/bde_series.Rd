% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bde-series.R
\encoding{UTF-8}
\name{bde_series}
\alias{bde_series}
\alias{bde_series_load}
\alias{bde_series_full_load}
\title{Load BdE time series from bulk CSV files}
\usage{
bde_series_load(
  series_code,
  series_label = NULL,
  out_format = "wide",
  parse_dates = TRUE,
  parse_numeric = TRUE,
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE,
  extract_metadata = FALSE
)

bde_series_full_load(
  series_csv,
  parse_dates = TRUE,
  parse_numeric = TRUE,
  cache_dir = NULL,
  update_cache = FALSE,
  verbose = FALSE,
  extract_metadata = FALSE
)
}
\arguments{
\item{series_code}{Numeric vector of BdE sequential numbers, or values
coercible with \code{\link[base:as.double]{base::as.double()}}, from the \verb{Número secuencial} field of
the corresponding series. This is not the API series code. See
\code{\link[=bde_catalog_load]{bde_catalog_load()}}.}

\item{series_label}{Optional character string or vector of labels to assign
to the extracted series.}

\item{out_format}{The format to return, either \code{"wide"} or \code{"long"}. See
\strong{Value} for details and the \strong{Examples} section.}

\item{parse_dates}{Logical. If \code{TRUE}, date columns are parsed with
\code{\link[=bde_parse_dates]{bde_parse_dates()}}.}

\item{parse_numeric}{Logical. If \code{TRUE}, the columns are parsed to
double (numeric) values. See \strong{Note}.}

\item{cache_dir}{Path to a cache directory. The directory can also be set
with \code{options(bde_cache_dir = "path/to/dir")}.}

\item{update_cache}{Logical. If \code{TRUE}, the requested file is refreshed in
\code{cache_dir}.}

\item{verbose}{Logical. If \code{TRUE}, display information useful for debugging.}

\item{extract_metadata}{Logical. If \code{TRUE}, the output is the metadata of the
requested series.}

\item{series_csv}{CSV file name for a series, as defined in the field
\verb{Nombre del archivo con los valores de la serie} of the corresponding
catalog. See \code{\link[=bde_catalog_load]{bde_catalog_load()}}.}
}
\value{
\code{bde_series_load()} returns a \link[tibble:tbl_df]{tibble} with a \code{Date} column:
\itemize{
\item With \code{out_format = "wide"}, each series is presented in a separate column
with the name defined by \code{series_label}.
\item With \code{out_format = "long"}, the tibble has two additional columns:
\code{serie_name}, with the label of each series and \code{serie_value}, with the
corresponding value.
}

\code{"wide"} format is more suitable for exporting to a \code{.csv} file, while
\code{"long"} format is more suitable for creating plots using
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}. See also \code{\link[tidyr:pivot_longer]{tidyr::pivot_longer()}} and
\code{\link[tidyr:pivot_wider]{tidyr::pivot_wider()}}.

\code{bde_series_full_load()} returns a \link[tibble:tbl_df]{tibble} with a \code{Date}
column and the aliases of the time series columns as described in catalog
metadata. See \code{\link[=bde_catalog_load]{bde_catalog_load()}} and
\code{vignette("csv_manual", package = "tidyBdE")} for details.
}
\description{
These functions load BdE time series from the bulk CSV files published by
Banco de España.

\code{bde_series_load()} extracts one or more series by their stable sequential
number. \code{bde_series_full_load()} loads a complete CSV file and returns all
series included in that file.

The series alias is a positional code showing the location, column and/or
row, of the series in the table. Although it is unique, it is not stable
enough to identify a time series because it may change when a series moves.

BdE also assigns each series a stable sequential number
(\verb{Número secuencial}). \code{bde_series_load()} uses that sequential number
through \code{series_code}, not the API series code used by the Statistics web
service (API) helpers.

A single time series may appear in different tables, so it can have several
aliases. Use \code{bde_series_full_load()} when you need to work with aliases or
load a complete file.
\subsection{About BdE file naming}{

The series alias is also used in full CSV files. For example, table
\strong{be_6_1} represents Table 1, Chapter 6 of the Statistical Bulletin
("BE"). Although it is unique, it is subject to change, for example when a
new table is inserted before it.
}
}
\note{
These functions attempt to coerce the columns to numbers. For some time
series, a warning may be displayed if parsing fails. You can override
the default behavior with \code{parse_numeric = FALSE}.
}
\examples{
\dontshow{if (bde_check_access()) withAutoprint(\{ # examplesIf}
\donttest{
# Show metadata.
bde_series_load(573234, verbose = TRUE, extract_metadata = TRUE)

# Load data.
bde_series_load(573234, extract_metadata = FALSE)

# Load multiple series.
bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR"),
  extract_metadata = TRUE
)

wide <- bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR")
)

# Show wide output.
wide

# Show long output.
long <- bde_series_load(c(573234, 573214),
  series_label = c("US/EUR", "GBP/EUR"),
  out_format = "long"
)

long

# Use with `ggplot2`.
library(ggplot2)

ggplot(long, aes(Date, serie_value)) +
  geom_line(aes(group = serie_name, color = serie_name)) +
  scale_color_bde_d() +
  theme_tidybde()

# Show metadata for a full file.
bde_series_full_load("TI_1_1.csv", extract_metadata = TRUE)

# Load a full file.
bde_series_full_load("TI_1_1.csv")
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=bde_catalog_load]{bde_catalog_load()}}, \code{\link[=bde_catalog_search]{bde_catalog_search()}},
\code{\link[=bde_indicators]{bde_indicators()}}

Series functions:
\code{\link{bde_series_api}}
}
\concept{series}
